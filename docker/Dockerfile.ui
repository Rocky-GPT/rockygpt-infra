# The Next.js web app. Talks to the brain over BRAIN_URL, never in process.

FROM node:22-slim AS build
WORKDIR /srv

RUN apt-get update \
  && apt-get install -y --no-install-recommends git ca-certificates \
  && rm -rf /var/lib/apt/lists/* \
  && git config --global url."https://github.com/".insteadOf "git@github.com:" \
  && git config --global url."https://github.com/".insteadOf "ssh://git@github.com/"

COPY package.json package-lock.json ./

ENV PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1
ENV NEXT_TELEMETRY_DISABLED=1
RUN npm ci --include=dev

COPY . .

# NEXT_PUBLIC_ values are inlined into the browser bundle when the app is
# built, not read when it runs, so the data service's address has to be known
# here. It is the address a browser will use, which is the published port on
# the host rather than the name of a service on the compose network.
ARG NEXT_PUBLIC_DATA_URL=http://localhost:8100
ENV NEXT_PUBLIC_DATA_URL=${NEXT_PUBLIC_DATA_URL}

# Every page that reads the database is force-dynamic, so nothing is
# prerendered here and the build needs no DATABASE_URL.
RUN npm run build && npm prune --omit=dev

FROM node:22-slim AS runtime
WORKDIR /srv
ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1

COPY --from=build /srv/node_modules ./node_modules
COPY --from=build /srv/.next ./.next
COPY --from=build /srv/public ./public
COPY --from=build /srv/package.json ./package.json
COPY --from=build /srv/next.config.ts ./next.config.ts
COPY --from=build /srv/lib ./lib

ENV PORT=3000
EXPOSE 3000

HEALTHCHECK --interval=30s --timeout=3s --start-period=20s --retries=5 \
  CMD node -e "fetch('http://127.0.0.1:3000/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))"

CMD ["npm", "start"]
